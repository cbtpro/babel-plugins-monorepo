{
  "name": "babel-plugins-monorepo",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "catalog": "pnpx codemod pnpm/catalog"
  }
}
